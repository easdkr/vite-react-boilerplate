import axiosHttpClient from 'src/apis/application/axios.http-client'
import { IHttpClient } from 'src/apis/core/http.client'

const baseHttpClient: IHttpClient = axiosHttpClient

export default baseHttpClient
